name: Build with vcpkg

on:
  push:
    branches:
      - master
      - v*

    paths:
      - .github/workflows/build-vcpkg.yml
      - cmake/**
      - src/**
      - CMakeLists.txt
      - vcpkg.json

  pull_request:
    paths:
      - .github/workflows/build-vcpkg.yml
      - cmake/**
      - src/**
      - CMakeLists.txt
      - vcpkg.json

jobs:
  build-vcpkg:
    name: ${{ matrix.name }}-${{ matrix.buildtype }}-luajit=${{ matrix.luajit }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        name: [ubuntu, macos, windows]
        buildtype: [Debug, Release]
        luajit: [on, off]
        include:
          - name: ubuntu
            os: ubuntu-24.04
            triplet: x64-linux
          - name: macos
            os: macos-latest
            triplet: arm64-osx
          - name: windows
            os: windows-2022
            triplet: x64-windows
        exclude:
          # LuaJIT is pulled from vcpkg on Windows and is always enabled there.
          - name: windows
            luajit: off

    env:
      VCPKG_DEFAULT_TRIPLET: ${{ matrix.triplet }}
      VCPKG_BUILD_TYPE: release

    steps:
      - uses: actions/checkout@v7

      - name: Get latest CMake
        # Using 'latest' branch, the latest CMake is installed.
        uses: lukka/get-cmake@latest

      - name: Ubuntu - install luajit, remove libmysqlclient-dev
        run: |
          sudo apt-get update
          sudo apt-get install -y libluajit-5.1-dev
          sudo apt-get remove -y libmysqlclient-dev
        if: matrix.name == 'ubuntu'

      - name: macOS - install luajit pkgconfig
        run: brew install luajit pkgconfig
        if: matrix.name == 'macos'

      - name: Windows - remove C:/mysql*
        run: rm -r -fo C:/mysql*
        if: matrix.name == 'windows'

      - name: Run vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          runVcpkgInstall: true

      # run-cmake@v10 only drives CMakePresets.json, which this branch does not
      # have, so cmake is invoked directly. The build directory stays outside
      # the workspace because the datapack step below deletes the workspace
      # contents before collecting the binary.
      - name: Configure with cmake
        run: >
          cmake -B ../build -S .
          -DCMAKE_BUILD_TYPE=${{ matrix.buildtype }}
          -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake
          -DVCPKG_TARGET_TRIPLET=${{ matrix.triplet }}
          -DUSE_LUAJIT=${{ matrix.luajit }}
        shell: bash

      - name: Build with cmake
        run: cmake --build ../build --config ${{ matrix.buildtype }} --parallel 2
        shell: bash

      # upload-artifact v4 and later reject a second upload to an existing
      # artifact name, so the binary and the datapack are collected into the
      # workspace and uploaded by a single step.
      - name: Prepare datapack contents
        run: |
          find . -maxdepth 1 ! -name data ! -name config.lua.dist ! -name key.pem ! -name LICENSE ! -name README.md ! -name schema.sql -exec rm -r {} \;
          # Single-config generators emit ../build/tfs, multi-config ones emit
          # ../build/<buildtype>/tfs.exe, so look one level deep either way.
          find ../build -maxdepth 2 -type f \( -name tfs -o -name tfs.exe -o -name '*.dll' \) -exec cp {} . \;
          [ -f tfs ] || [ -f tfs.exe ] || { echo "::error::no tfs binary was produced"; exit 1; }
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: tfs-${{ matrix.name }}-${{ matrix.buildtype }}-luajit=${{ matrix.luajit }}-${{ github.sha }}
          path: ${{ github.workspace }}
